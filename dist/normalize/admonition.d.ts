import { Editor, NodeEntry } from 'slate';
/**
 * Normalize an admonition
 *
 * Return true if entry was normalized and false otherwise.
 */
export default function normalizeAdmonition(editor: Editor, entry: NodeEntry): boolean;
